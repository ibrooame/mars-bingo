{
  "name": "mars-bingo-live",
  "version": "1.0.0",
  "description": "Production-grade real-time Telegram Mini App Bingo casino optimized for Termux.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "NODE_ENV=development node server/server.js",
    "init-db": "node -e \"require('./server/database/db').initDatabase().then(()=>process.exit(0)).catch(e=>{console.error(e);process.exit(1)})\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "node-cron": "^3.0.3",
    "socket.io": "^4.8.1",
    "sqlite3": "^5.1.7",
    "telegraf": "^4.16.3",
    "uuid": "^11.0.5"
  }
}
